# Test environment overrides
# Load this in addition to your base .env when running test mode.

# General
APP_ENV=test
TZ=UTC

# Paths (separate from dev to avoid mixing artifacts)
DATA_DIR=./var/test
SQLITE_PATH=./var/test/app.db

# Telegram (reuse from .env unless you want to override)
# TELEGRAM_TOKEN=...

# Registration / roles
# Set to your Telegram ID for owner testing; can be multiple, comma-separated
# OWNERS_TELEGRAM_ID=123456789

# Teacher registration secret (keep consistent with your scenario)
# COURSE_SECRET=TA-2025
